// Generated by tools/sync_action_metadata.py. Do not edit.
if (spec.name == "actions") {
    spec.description_en = "List the public xdebug action catalog.";
    spec.description_zh = "列出当前运行时公开 action catalog。";
    spec.purposes = {"discover"};
    spec.use_for = {"List the public xdebug action catalog."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "apb.config.list") {
    spec.description_en = "List saved APB interface configurations.";
    spec.description_zh = "列出 APB 配置。";
    spec.purposes = {"discover"};
    spec.use_for = {"List saved APB interface configurations."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "apb.config.load") {
    spec.description_en = "Load and validate an APB interface configuration.";
    spec.description_zh = "加载 APB 配置。";
    spec.purposes = {"configure"};
    spec.use_for = {"Load and validate an APB interface configuration."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "apb.cursor") {
    spec.description_en = "Move a cursor across APB transfers.";
    spec.description_zh = "在 APB transfer 间移动游标。";
    spec.purposes = {"query"};
    spec.use_for = {"Move a cursor across APB transfers."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "apb.query") {
    spec.description_en = "Query APB read and write transfers.";
    spec.description_zh = "查询 APB transfer。";
    spec.purposes = {"query"};
    spec.use_for = {"Query APB read and write transfers."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "apb.transfer_window") {
    spec.description_en = "Inspect the waveform window around an APB transfer.";
    spec.description_zh = "实验性 APB 窗口分析。";
    spec.purposes = {"analyze"};
    spec.use_for = {"Inspect the waveform window around an APB transfer."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "axi.analysis") {
    spec.description_en = "Analyze AXI latency, pending transactions, and outstanding activity.";
    spec.description_zh = "汇总 AXI 行为。";
    spec.purposes = {"analyze"};
    spec.use_for = {"Analyze AXI latency, pending transactions, and outstanding activity."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "axi.export") {
    spec.description_en = "Export AXI transactions for external analysis.";
    spec.description_zh = "导出 AXI 数据。";
    spec.purposes = {"export"};
    spec.use_for = {"Export AXI transactions for external analysis."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "axi.channel_stall") {
    spec.description_en = "Inspect AXI channel stall windows.";
    spec.description_zh = "实验性 AXI stall 分析。";
    spec.purposes = {"query"};
    spec.use_for = {"Inspect AXI channel stall windows."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "axi.config.list") {
    spec.description_en = "List saved AXI interface configurations.";
    spec.description_zh = "列出 AXI 配置。";
    spec.purposes = {"discover"};
    spec.use_for = {"List saved AXI interface configurations."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "axi.config.load") {
    spec.description_en = "Load and validate an AXI interface configuration.";
    spec.description_zh = "加载 AXI 配置。";
    spec.purposes = {"configure"};
    spec.use_for = {"Load and validate an AXI interface configuration."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "axi.cursor") {
    spec.description_en = "Move a cursor across AXI transactions.";
    spec.description_zh = "在 AXI transfer 间移动游标。";
    spec.purposes = {"query"};
    spec.use_for = {"Move a cursor across AXI transactions."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "axi.latency_outlier") {
    spec.description_en = "Find AXI transactions with abnormal latency.";
    spec.description_zh = "实验性 AXI latency 异常。";
    spec.purposes = {"analyze"};
    spec.use_for = {"Find AXI transactions with abnormal latency."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "axi.outstanding_timeline") {
    spec.description_en = "Analyze the AXI pending-transaction timeline.";
    spec.description_zh = "实验性 AXI outstanding 时间线。";
    spec.purposes = {"analyze"};
    spec.use_for = {"Analyze the AXI pending-transaction timeline."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "axi.query") {
    spec.description_en = "Query AXI channels and reconstructed transactions.";
    spec.description_zh = "查询 AXI channel/transaction。";
    spec.purposes = {"query"};
    spec.use_for = {"Query AXI channels and reconstructed transactions."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "axi.request_response_pair") {
    spec.description_en = "Pair AXI requests with their responses.";
    spec.description_zh = "实验性 AXI 请求响应配对。";
    spec.purposes = {"analyze"};
    spec.use_for = {"Pair AXI requests with their responses."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "batch") {
    spec.description_en = "Execute multiple xdebug requests as one ordered batch.";
    spec.description_zh = "批量执行多个 xdebug request。";
    spec.purposes = {"orchestrate"};
    spec.use_for = {"Execute multiple xdebug requests as one ordered batch."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "counter.statistics") {
    spec.description_en = "Analyze sampled counter behavior and statistics.";
    spec.description_zh = "统计计数器行为。";
    spec.purposes = {"analyze"};
    spec.use_for = {"Analyze sampled counter behavior and statistics."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "cursor.delete") {
    spec.description_en = "Delete a named waveform cursor.";
    spec.description_zh = "删除游标。";
    spec.purposes = {"manage"};
    spec.use_for = {"Delete a named waveform cursor."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "cursor.get") {
    spec.description_en = "Read a named waveform cursor.";
    spec.description_zh = "读取命名游标。";
    spec.purposes = {"query", "manage"};
    spec.use_for = {"Read a named waveform cursor."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "cursor.list") {
    spec.description_en = "List named waveform cursors.";
    spec.description_zh = "列出游标。";
    spec.purposes = {"discover", "manage"};
    spec.use_for = {"List named waveform cursors."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "cursor.set") {
    spec.description_en = "Save a named waveform cursor.";
    spec.description_zh = "保存命名时间游标。";
    spec.purposes = {"configure", "manage"};
    spec.use_for = {"Save a named waveform cursor."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "cursor.use") {
    spec.description_en = "Activate a named waveform cursor.";
    spec.description_zh = "激活游标。";
    spec.purposes = {"configure", "manage"};
    spec.use_for = {"Activate a named waveform cursor."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "detect_abnormal") {
    spec.description_en = "Detect waveform glitches, unknown values, and stuck signals.";
    spec.description_zh = "扫描常见波形异常。";
    spec.purposes = {"inspect"};
    spec.use_for = {"Detect waveform glitches, unknown values, and stuck signals."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "event.config.list") {
    spec.description_en = "List saved event-query configurations.";
    spec.description_zh = "列出事件配置。";
    spec.purposes = {"discover"};
    spec.use_for = {"List saved event-query configurations."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "event.config.load") {
    spec.description_en = "Load an event-query configuration.";
    spec.description_zh = "保存事件查询配置。";
    spec.purposes = {"configure"};
    spec.use_for = {"Load an event-query configuration."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "event.export") {
    spec.description_en = "Export sampled events that match an expression.";
    spec.description_zh = "导出满足表达式的事件。";
    spec.purposes = {"export"};
    spec.use_for = {"Export sampled events that match an expression."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "event.find") {
    spec.description_en = "Find sampled events that match an expression.";
    spec.description_zh = "查找满足表达式的事件样例。";
    spec.purposes = {"query"};
    spec.use_for = {"Find sampled events that match an expression."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "expr.eval_at") {
    spec.description_en = "Evaluate an expression at a sampled waveform time.";
    spec.description_zh = "在指定时间求布尔/表达式值。";
    spec.purposes = {"transform"};
    spec.use_for = {"Evaluate an expression at a sampled waveform time."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "expr.normalize") {
    spec.description_en = "Normalize an expression for deterministic processing.";
    spec.description_zh = "规范化表达式。";
    spec.purposes = {"transform"};
    spec.use_for = {"Normalize an expression for deterministic processing."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "handshake.inspect") {
    spec.description_en = "Inspect valid-ready transfers, stalls, and stability violations.";
    spec.description_zh = "检查 valid/ready 握手。";
    spec.purposes = {"inspect"};
    spec.use_for = {"Inspect valid-ready transfers, stalls, and stability violations."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "list.add") {
    spec.description_en = "Add a signal to a named waveform list.";
    spec.description_zh = "向信号列表追加一个信号。";
    spec.purposes = {"configure", "manage"};
    spec.use_for = {"Add a signal to a named waveform list."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "list.create") {
    spec.description_en = "Create a named waveform signal list.";
    spec.description_zh = "创建命名信号列表。";
    spec.purposes = {"configure", "manage"};
    spec.use_for = {"Create a named waveform signal list."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "list.delete") {
    spec.description_en = "Delete a signal or index from a waveform list.";
    spec.description_zh = "从信号列表删除信号或 index。";
    spec.purposes = {"configure", "manage"};
    spec.use_for = {"Delete a signal or index from a waveform list."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "list.diff") {
    spec.description_en = "Find the first difference in a waveform list over a time range.";
    spec.description_zh = "查找 list 在时间窗口内的首次差异。";
    spec.purposes = {"analyze"};
    spec.use_for = {"Find the first difference in a waveform list over a time range."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "list.export") {
    spec.description_en = "Export waveform-list data.";
    spec.description_zh = "导出 list 数据。";
    spec.purposes = {"export"};
    spec.use_for = {"Export waveform-list data."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "list.show") {
    spec.description_en = "Show the contents of a waveform signal list.";
    spec.description_zh = "显示信号列表内容。";
    spec.purposes = {"query"};
    spec.use_for = {"Show the contents of a waveform signal list."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "list.validate") {
    spec.description_en = "Validate signal paths stored in a waveform list.";
    spec.description_zh = "验证 list 中信号是否存在。";
    spec.purposes = {"verify"};
    spec.use_for = {"Validate signal paths stored in a waveform list."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "list.value_at") {
    spec.description_en = "Read all signals in a waveform list at one sampled time.";
    spec.description_zh = "读取 list 内所有信号在指定时间的值。";
    spec.purposes = {"query"};
    spec.use_for = {"Read all signals in a waveform list at one sampled time."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "sampled_pulse.inspect") {
    spec.description_en = "Inspect valid pulses that were not observed at sampling edges.";
    spec.description_zh = "检查未被 clock 采到的 valid 短脉冲。";
    spec.purposes = {"inspect"};
    spec.use_for = {"Inspect valid pulses that were not observed at sampling edges."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "schema") {
    spec.description_en = "Return the request or response schema for an action.";
    spec.description_zh = "返回指定 action 的 request/response JSON schema。";
    spec.purposes = {"discover"};
    spec.use_for = {"Return the request or response schema for an action."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "scope.list") {
    spec.description_en = "List waveform scopes and signals.";
    spec.description_zh = "列出 FSDB scope 或信号。";
    spec.purposes = {"discover"};
    spec.use_for = {"List waveform scopes and signals."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "scope.roots") {
    spec.description_en = "Discover waveform and design hierarchy roots.";
    spec.description_zh = "发现 waveform/design 根。";
    spec.purposes = {"discover"};
    spec.use_for = {"Discover waveform and design hierarchy roots."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "session.close") {
    spec.description_en = "Close a debug session and release its runtime resources.";
    spec.description_zh = "关闭指定 session。";
    spec.purposes = {"manage"};
    spec.use_for = {"Close a debug session and release its runtime resources."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "session.doctor") {
    spec.description_en = "Diagnose debug-session resources and accessibility.";
    spec.description_zh = "诊断当前 session。";
    spec.purposes = {"manage"};
    spec.use_for = {"Diagnose debug-session resources and accessibility."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "session.gc") {
    spec.description_en = "Garbage-collect expired debug sessions.";
    spec.description_zh = "清理过期 session。";
    spec.purposes = {"manage"};
    spec.use_for = {"Garbage-collect expired debug sessions."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "session.kill") {
    spec.description_en = "Force removal of a debug session.";
    spec.description_zh = "强制移除指定 session。";
    spec.purposes = {"manage"};
    spec.use_for = {"Force removal of a debug session."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "session.list") {
    spec.description_en = "List active debug sessions.";
    spec.description_zh = "列出当前 session。";
    spec.purposes = {"discover", "manage"};
    spec.use_for = {"List active debug sessions."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "session.open") {
    spec.description_en = "Open a design, waveform, or combined debug session.";
    spec.description_zh = "打开 design/waveform session。";
    spec.purposes = {"manage"};
    spec.use_for = {"Open a design, waveform, or combined debug session."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "signal.canonicalize") {
    spec.description_en = "Return the canonical design path for a signal.";
    spec.description_zh = "返回信号 canonical 名称。";
    spec.purposes = {"transform"};
    spec.use_for = {"Return the canonical design path for a signal."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "signal.changes") {
    spec.description_en = "List raw waveform value changes for a signal.";
    spec.description_zh = "读取信号变化点。";
    spec.purposes = {"query"};
    spec.use_for = {"List raw waveform value changes for a signal."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "signal.resolve") {
    spec.description_en = "Resolve a design signal to canonical candidates.";
    spec.description_zh = "解析设计信号。";
    spec.purposes = {"transform"};
    spec.use_for = {"Resolve a design signal to canonical candidates."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "signal.stability") {
    spec.description_en = "Check whether a signal stays stable in a time range.";
    spec.description_zh = "检查信号窗口内是否稳定。";
    spec.purposes = {"analyze"};
    spec.use_for = {"Check whether a signal stays stable in a time range."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "signal.statistics") {
    spec.description_en = "Measure waveform signal activity and sampled values.";
    spec.description_zh = "统计信号活动。";
    spec.purposes = {"analyze"};
    spec.use_for = {"Measure waveform signal activity and sampled values."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "source.context") {
    spec.description_en = "Read source code around a file and line.";
    spec.description_zh = "读取源码上下文。";
    spec.purposes = {"query"};
    spec.use_for = {"Read source code around a file and line."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "trace.active_driver") {
    spec.description_en = "Find active driver evidence at a specific waveform time.";
    spec.description_zh = "在指定时间找 active driver。";
    spec.purposes = {"trace"};
    spec.use_for = {"Find active driver evidence at a specific waveform time."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "trace.active_driver_chain") {
    spec.description_en = "Trace an active-driver chain across multiple levels.";
    spec.description_zh = "展开 active driver 链。";
    spec.purposes = {"trace"};
    spec.use_for = {"Trace an active-driver chain across multiple levels."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "trace.driver") {
    spec.description_en = "Find static drivers of a design signal.";
    spec.description_zh = "查找信号直接 driver。";
    spec.purposes = {"trace"};
    spec.use_for = {"Find static drivers of a design signal."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "trace.load") {
    spec.description_en = "Find static loads of a design signal.";
    spec.description_zh = "查找信号 load。";
    spec.purposes = {"trace"};
    spec.use_for = {"Find static loads of a design signal."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "rc.generate") {
    spec.description_en = "Generate a grouped waveform-view RC file.";
    spec.description_zh = "根据分组生成波形 rc。";
    spec.purposes = {"export"};
    spec.use_for = {"Generate a grouped waveform-view RC file."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "value.at") {
    spec.description_en = "Read one signal value at a sampled waveform time.";
    spec.description_zh = "读取单个信号在指定时间的值。";
    spec.purposes = {"query"};
    spec.use_for = {"Read one signal value at a sampled waveform time."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "value.batch_at") {
    spec.description_en = "Read multiple signal values at one sampled waveform time.";
    spec.description_zh = "批量读取多个信号值。";
    spec.purposes = {"query"};
    spec.use_for = {"Read multiple signal values at one sampled waveform time."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "verify.conditions") {
    spec.description_en = "Verify multiple conditions at one sampled time.";
    spec.description_zh = "在单个时间验证条件集合。";
    spec.purposes = {"verify"};
    spec.use_for = {"Verify multiple conditions at one sampled time."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "window.verify") {
    spec.description_en = "Verify conditions across a clock-sampled time window.";
    spec.description_zh = "按 clock 在窗口内验证条件。";
    spec.purposes = {"verify"};
    spec.use_for = {"Verify conditions across a clock-sampled time window."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "stream.config.load") {
    spec.description_en = "Load and validate valid-ready stream configurations.";
    spec.description_zh = "加载 stream 配置。";
    spec.purposes = {"configure"};
    spec.use_for = {"Load and validate valid-ready stream configurations."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "stream.config.list") {
    spec.description_en = "List saved stream configurations.";
    spec.description_zh = "列出 stream 配置。";
    spec.purposes = {"discover"};
    spec.use_for = {"List saved stream configurations."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "stream.show") {
    spec.description_en = "Show a stream definition and its resolved signal metadata.";
    spec.description_zh = "显示 stream 定义和摘要。";
    spec.purposes = {"query"};
    spec.use_for = {"Show a stream definition and its resolved signal metadata."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "stream.validate") {
    spec.description_en = "Validate stream configuration and observed behavior.";
    spec.description_zh = "验证 stream 配置。";
    spec.purposes = {"verify"};
    spec.use_for = {"Validate stream configuration and observed behavior."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "stream.query") {
    spec.description_en = "Query stream transfers, stalls, packets, and fields.";
    spec.description_zh = "查询 stream transfer。";
    spec.purposes = {"query"};
    spec.use_for = {"Query stream transfers, stalls, packets, and fields."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
if (spec.name == "stream.export") {
    spec.description_en = "Export stream transfers or packet data.";
    spec.description_zh = "导出 stream 查询结果。";
    spec.purposes = {"export"};
    spec.use_for = {"Export stream transfers or packet data."};
    spec.do_not_use_for = {"Tasks outside this action's documented contract."};
    spec.preferred_alternative = Json::object();
    return;
}
