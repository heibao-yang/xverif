{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.stream.validate.request.v1",
  "title": "stream.validate request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "action": {
      "type": "string",
      "enum": [
        "stream.validate"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "required": [
        "stream"
      ],
      "properties": {
        "channel": {
          "type": "string",
          "description": "Action-specific channel input. Its type and accepted values are defined by this schema."
        },
        "dynamic": {
          "type": "boolean",
          "description": "Action-specific dynamic input. Its type and accepted values are defined by this schema."
        },
        "line_limit": {
          "type": "integer",
          "minimum": 1,
          "description": "Limits returned evidence rows only, not scanning, aggregation, or the verdict; read completeness fields as well."
        },
        "stream": {
          "type": "string",
          "description": "Action-specific stream input. Its type and accepted values are defined by this schema."
        },
        "time_range": {
          "type": "object",
          "properties": {
            "begin": {
              "type": "string",
              "description": "Action-specific begin input. Its type and accepted values are defined by this schema."
            },
            "end": {
              "type": "string",
              "description": "Action-specific end input. Its type and accepted values are defined by this schema."
            }
          },
          "additionalProperties": false,
          "description": "Closed analysis interval. begin and end may be omitted independently to use the available waveform bounds."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        }
      },
      "additionalProperties": false
    },
    "limits": {
      "type": "object"
    },
    "request_id": {
      "type": "string"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Validate stream configuration and observed behavior.",
  "x-purpose": "验证 stream 配置。",
  "x-how_it_works": "检查 stream 信号在 FSDB 中是否可解析。",
  "x-when_to_use": "提前发现错误信号路径。",
  "x-arg_contract_notes": "required: stream",
  "x-description-zh": "验证 stream 配置。",
  "x-agent": {
    "use_when": [
      "验证 stream 配置。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 stream.config.load、stream.config.list 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "stream.config.load",
        "when": "加载 stream 配置。"
      },
      {
        "action": "stream.config.list",
        "when": "列出 stream 配置。"
      }
    ],
    "constraints": [
      "required: stream"
    ]
  }
}
