{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.stream.validate.request.v1",
  "title": "stream.validate request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "action": {
      "type": "string",
      "enum": [
        "stream.validate"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "required": [
        "stream"
      ],
      "properties": {
        "begin": {
          "type": "string",
          "description": "查询窗口起始时间。"
        },
        "channel": {
          "type": "string"
        },
        "dynamic": {
          "type": "boolean"
        },
        "end": {
          "type": "string",
          "description": "查询窗口结束时间。"
        },
        "limit": {
          "type": "integer"
        },
        "max_edges": {
          "type": "integer"
        },
        "name": {
          "type": "string",
          "description": "已保存配置、游标、列表或接口配置名称。"
        },
        "start": {
          "type": "string",
          "description": "查询窗口起始时间。"
        },
        "stream": {
          "type": "string",
          "description": "已保存 stream 配置名称。"
        },
        "time_range": {
          "type": "object",
          "description": "查询或分析的时间窗口。"
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ]
        }
      },
      "additionalProperties": false
    },
    "limits": {
      "type": "object"
    },
    "output": {
      "type": "object"
    },
    "request_id": {
      "type": "string"
    },
    "auth_token": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "stream.validate: 验证 stream 配置。",
  "x-purpose": "验证 stream 配置。",
  "x-how_it_works": "检查 stream 信号在 FSDB 中是否可解析。",
  "x-when_to_use": "提前发现错误信号路径。",
  "x-arg_contract_notes": "required: stream"
}
