{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.signal.stability.request.v1",
  "title": "signal.stability request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "signal.stability"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "conditions": {
          "type": "array",
          "items": {
            "type": "object",
            "description": "Structured conditions configuration. Only the declared properties are accepted."
          },
          "description": "Ordered conditions entries. Each item must satisfy the declared item contract."
        },
        "mode": {
          "type": "string",
          "description": "Action processing or return mode. Its legal values, default, and interactions are action-specific."
        },
        "signal": {
          "type": "string",
          "description": "Final leaf signal path. Aggregate, array, and struct roots are not expanded automatically."
        },
        "signals": {
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string",
                "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
              },
              "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
            },
            {
              "type": "object",
              "additionalProperties": {
                "type": "string",
                "description": "Value for a caller-defined signal alias key. Supply the real signal path or the action-supported expression for that alias.",
                "x-dynamic-contract": "Each property key is an alias referenced by this action; each value resolves that alias to a signal path or supported expression."
              },
              "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
            }
          ],
          "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
        },
        "time_range": {
          "type": "object",
          "properties": {
            "begin": {
              "type": "string",
              "description": "Action-specific begin input. Its type and accepted values are defined by this schema."
            },
            "end": {
              "type": "string",
              "description": "Action-specific end input. Its type and accepted values are defined by this schema."
            }
          },
          "additionalProperties": false,
          "description": "Closed analysis interval. begin and end may be omitted independently to use the available waveform bounds."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        }
      },
      "additionalProperties": false,
      "required": [
        "signal"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Check whether a signal stays stable in a time range.",
  "x-purpose": "检查信号窗口内是否稳定。",
  "x-how_it_works": "基于 signal.changes 判断是否只有初始值或无变化。",
  "x-when_to_use": "确认控制/状态信号是否保持不变。",
  "x-arg_contract_notes": "required: signal",
  "x-description-zh": "检查信号窗口内是否稳定。",
  "x-agent": {
    "use_when": [
      "检查信号窗口内是否稳定。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 signal.canonicalize、signal.changes 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "signal.canonicalize",
        "when": "返回信号 canonical 名称。"
      },
      {
        "action": "signal.changes",
        "when": "读取信号变化点。"
      }
    ],
    "constraints": [
      "required: signal"
    ]
  }
}
