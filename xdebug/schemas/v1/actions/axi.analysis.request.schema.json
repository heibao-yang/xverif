{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.axi.analysis.request.v1",
  "title": "axi.analysis request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "axi.analysis"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "analysis": {
          "type": "string",
          "enum": [
            "latency",
            "osd",
            "outstanding"
          ]
        },
        "direction": {
          "type": "string",
          "enum": [
            "wr",
            "rd",
            "all"
          ]
        },
        "id": {
          "type": "string",
          "description": "目标 xdebug session 标识；session_id 的兼容别名。"
        },
        "include_transactions": {
          "type": "boolean"
        },
        "name": {
          "type": "string",
          "description": "已保存配置、游标、列表或接口配置名称。"
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "limits": {
      "type": "object"
    },
    "output": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "axi.analysis: 汇总 AXI 行为。",
  "x-purpose": "汇总 AXI 行为。",
  "x-how_it_works": "基于 AXI 解析结果统计 channel、latency、stall 等。",
  "x-when_to_use": "快速判断 AXI 接口健康度。",
  "x-arg_contract_notes": "required: name"
}
