{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.list.create.request.v1",
  "title": "list.create request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "list.create"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Saved-object name in this action namespace; do not assume names are shared by cursors, lists, and protocol configs."
        },
        "signals": {
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string",
                "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
              },
              "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
            },
            {
              "type": "object",
              "additionalProperties": {
                "type": "string",
                "description": "Value for a caller-defined signal alias key. Supply the real signal path or the action-supported expression for that alias.",
                "x-dynamic-contract": "Each property key is an alias referenced by this action; each value resolves that alias to a signal path or supported expression."
              },
              "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
            }
          ],
          "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Create a named waveform signal list.",
  "x-purpose": "创建命名信号列表。",
  "x-how_it_works": "在 ListManager 存储中创建 list，并可追加初始 signals。",
  "x-when_to_use": "为一组信号建立可复用集合。",
  "x-arg_contract_notes": "required: name",
  "x-description-zh": "创建命名信号列表。",
  "x-agent": {
    "use_when": [
      "创建命名信号列表。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 list.add、list.delete 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "list.add",
        "when": "向信号列表追加一个信号。"
      },
      {
        "action": "list.delete",
        "when": "从信号列表删除信号或 index。"
      }
    ],
    "constraints": [
      "required: name"
    ]
  }
}
