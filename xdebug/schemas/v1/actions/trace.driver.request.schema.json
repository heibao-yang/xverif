{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.trace.driver.request.v1",
  "title": "trace.driver request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "trace.driver"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "line_limit": {
          "type": "integer",
          "minimum": 1,
          "description": "Limits returned evidence rows only, not scanning, aggregation, or the verdict; read completeness fields as well."
        },
        "no_statement_only": {
          "type": "boolean",
          "description": "Action-specific no_statement_only input. Its type and accepted values are defined by this schema."
        },
        "role": {
          "type": "string",
          "description": "Action-specific role input. Its type and accepted values are defined by this schema."
        },
        "signal": {
          "type": "string",
          "description": "Final leaf signal path. Aggregate, array, and struct roots are not expanded automatically."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        }
      },
      "additionalProperties": false,
      "required": [
        "signal"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Find static drivers of a design signal.",
  "x-purpose": "查找信号直接 driver。",
  "x-how_it_works": "调用设计 trace 能力分析赋值、依赖和来源。",
  "x-when_to_use": "解释某个信号由什么驱动。",
  "x-arg_contract_notes": "required: signal",
  "x-description-zh": "查找信号直接 driver。",
  "x-agent": {
    "use_when": [
      "查找信号直接 driver。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 trace.active_driver、trace.active_driver_chain 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "trace.active_driver",
        "when": "在指定时间找 active driver。"
      },
      {
        "action": "trace.active_driver_chain",
        "when": "展开 active driver 链。"
      }
    ],
    "constraints": [
      "required: signal"
    ]
  }
}
