{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.apb.query.request.v1",
  "title": "apb.query request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "apb.query"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "addr": {
          "type": "string",
          "description": "Action-specific addr input. Its type and accepted values are defined by this schema."
        },
        "address": {
          "type": "string",
          "description": "Action-specific address input. Its type and accepted values are defined by this schema."
        },
        "direction": {
          "type": "string",
          "enum": [
            "write",
            "read",
            "all"
          ],
          "description": "Action-specific direction input. Its type and accepted values are defined by this schema."
        },
        "last": {
          "type": "boolean",
          "description": "Action-specific last input. Its type and accepted values are defined by this schema."
        },
        "name": {
          "type": "string",
          "description": "Saved-object name in this action namespace; do not assume names are shared by cursors, lists, and protocol configs."
        },
        "query": {
          "type": "object",
          "properties": {
            "line_limit": {
              "type": "integer",
              "minimum": 1,
              "description": "Limits returned evidence rows only, not scanning, aggregation, or the verdict; read completeness fields as well."
            },
            "index": {
              "type": "integer",
              "minimum": 1,
              "description": "One-based query, cursor, or list position; the action schema defines its exact reference set."
            }
          },
          "additionalProperties": false,
          "description": "Action-specific query selector. Do not copy index, channel, or filter forms from another action."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Query APB read and write transfers.",
  "x-purpose": "查询 APB transfer。",
  "x-how_it_works": "按 APB 配置扫描 PSEL/PENABLE/PREADY 等握手和地址数据；第 N 个 transfer 用 1-based query.index，多条用 query.line_limit。",
  "x-when_to_use": "抽取 APB 读写访问。",
  "x-arg_contract_notes": "required: name; use query.index / query.line_limit; do not use legacy quantity fields",
  "x-description-zh": "查询 APB transfer。",
  "x-agent": {
    "use_when": [
      "查询 APB transfer。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 apb.config.list、apb.config.load 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "apb.config.list",
        "when": "列出 APB 配置。"
      },
      {
        "action": "apb.config.load",
        "when": "加载 APB 配置。"
      }
    ],
    "constraints": [
      "required: name; use query.index / query.line_limit; do not use legacy quantity fields"
    ]
  }
}
