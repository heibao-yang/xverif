{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.detect_abnormal.request.v1",
  "title": "detect_abnormal request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "detect_abnormal"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "checks": {
          "type": "array",
          "items": {
            "description": "一项由 type 判别的 abnormal 检查。",
            "oneOf": [
              {
                "type": "object",
                "description": "Structured checks configuration. Only the declared properties are accepted.",
                "required": [
                  "type"
                ],
                "properties": {
                  "type": {
                    "const": "unknown_xz",
                    "description": "报告区间内出现的 X/Z。"
                  }
                },
                "additionalProperties": false
              },
              {
                "type": "object",
                "description": "Structured checks configuration. Only the declared properties are accepted.",
                "required": [
                  "type",
                  "min_pulse_width"
                ],
                "properties": {
                  "type": {
                    "const": "glitch",
                    "description": "选择短脉冲检查。"
                  },
                  "min_pulse_width": {
                    "type": "string",
                    "description": "Action-specific min_pulse_width input. Its type and accepted values are defined by this schema."
                  }
                },
                "additionalProperties": false
              },
              {
                "type": "object",
                "description": "Structured checks configuration. Only the declared properties are accepted.",
                "required": [
                  "type",
                  "min_duration"
                ],
                "properties": {
                  "type": {
                    "const": "stuck",
                    "description": "选择长时间不变检查。"
                  },
                  "min_duration": {
                    "type": "string",
                    "description": "Action-specific min_duration input. Its type and accepted values are defined by this schema."
                  }
                },
                "additionalProperties": false
              }
            ]
          },
          "description": "Ordered checks entries. Each item must satisfy the declared item contract.",
          "minItems": 1
        },
        "line_limit": {
          "type": "integer",
          "minimum": 1,
          "description": "Limits returned evidence rows only, not scanning, aggregation, or the verdict; read completeness fields as well."
        },
        "signals": {
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string",
                "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
              },
              "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
            },
            {
              "type": "object",
              "additionalProperties": {
                "type": "string",
                "description": "Value for a caller-defined signal alias key. Supply the real signal path or the action-supported expression for that alias.",
                "x-dynamic-contract": "Each property key is an alias referenced by this action; each value resolves that alias to a signal path or supported expression."
              },
              "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
            }
          ],
          "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
        },
        "time_range": {
          "type": "object",
          "properties": {
            "begin": {
              "type": "string",
              "description": "Action-specific begin input. Its type and accepted values are defined by this schema."
            },
            "end": {
              "type": "string",
              "description": "Action-specific end input. Its type and accepted values are defined by this schema."
            }
          },
          "additionalProperties": false,
          "description": "Closed analysis interval. begin and end may be omitted independently to use the available waveform bounds."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        },
        "value_format": {
          "type": "string",
          "enum": [
            "hex",
            "bin",
            "dec"
          ],
          "description": "Action-specific value_format input. Its type and accepted values are defined by this schema."
        }
      },
      "additionalProperties": false,
      "required": [
        "signals"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Detect waveform glitches, unknown values, and stuck signals.",
  "x-purpose": "扫描常见波形异常。",
  "x-how_it_works": "对 signals 执行 glitch/stuck/unknown 等检查并返回 findings；valid/ready 协议里的合法 idle/backpressure 不应只凭 stuck finding 判为 bug。",
  "x-when_to_use": "快速发现值得展开的异常窗口。",
  "x-arg_contract_notes": "required: signals",
  "x-description-zh": "扫描常见波形异常。",
  "x-agent": {
    "use_when": [
      "需要在 raw waveform 中检查 X/Z、短脉冲或长时间不变。"
    ],
    "do_not_use_when": [
      "需要证明 valid-ready 协议违规。"
    ],
    "alternatives": [
      {
        "action": "handshake.inspect",
        "when": "需要协议层 stall 或稳定性结论。"
      }
    ],
    "constraints": [
      "required: signals"
    ]
  }
}
