{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.axi.config.load.request.v1",
  "title": "axi.config.load request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "axi.config.load"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "config": {
          "type": "object",
          "description": "Structured config configuration. Only the declared properties are accepted.",
          "properties": {
            "clock": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific clock input. Its type and accepted values are defined by this schema."
            },
            "rst_n": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific rst_n input. Its type and accepted values are defined by this schema."
            },
            "awvalid": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific awvalid input. Its type and accepted values are defined by this schema."
            },
            "awready": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific awready input. Its type and accepted values are defined by this schema."
            },
            "awaddr": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific awaddr input. Its type and accepted values are defined by this schema."
            },
            "awid": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific awid input. Its type and accepted values are defined by this schema."
            },
            "awlen": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific awlen input. Its type and accepted values are defined by this schema."
            },
            "awsize": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific awsize input. Its type and accepted values are defined by this schema."
            },
            "awburst": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific awburst input. Its type and accepted values are defined by this schema."
            },
            "wvalid": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific wvalid input. Its type and accepted values are defined by this schema."
            },
            "wready": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific wready input. Its type and accepted values are defined by this schema."
            },
            "wdata": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific wdata input. Its type and accepted values are defined by this schema."
            },
            "wstrb": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific wstrb input. Its type and accepted values are defined by this schema."
            },
            "wlast": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific wlast input. Its type and accepted values are defined by this schema."
            },
            "bvalid": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific bvalid input. Its type and accepted values are defined by this schema."
            },
            "bready": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific bready input. Its type and accepted values are defined by this schema."
            },
            "bid": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific bid input. Its type and accepted values are defined by this schema."
            },
            "bresp": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific bresp input. Its type and accepted values are defined by this schema."
            },
            "arvalid": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific arvalid input. Its type and accepted values are defined by this schema."
            },
            "arready": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific arready input. Its type and accepted values are defined by this schema."
            },
            "araddr": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific araddr input. Its type and accepted values are defined by this schema."
            },
            "arid": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific arid input. Its type and accepted values are defined by this schema."
            },
            "arlen": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific arlen input. Its type and accepted values are defined by this schema."
            },
            "arsize": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific arsize input. Its type and accepted values are defined by this schema."
            },
            "arburst": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific arburst input. Its type and accepted values are defined by this schema."
            },
            "rvalid": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific rvalid input. Its type and accepted values are defined by this schema."
            },
            "rready": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific rready input. Its type and accepted values are defined by this schema."
            },
            "rdata": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific rdata input. Its type and accepted values are defined by this schema."
            },
            "rid": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific rid input. Its type and accepted values are defined by this schema."
            },
            "rresp": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific rresp input. Its type and accepted values are defined by this schema."
            },
            "rlast": {
              "type": "string",
              "minLength": 1,
              "description": "Action-specific rlast input. Its type and accepted values are defined by this schema."
            },
            "edge": {
              "type": "string",
              "enum": [
                "posedge",
                "negedge",
                "dual"
              ],
              "description": "Clock sampling edge. The schema default is authoritative; negedge often matches monitor semantics."
            },
            "sample_point": {
              "type": "string",
              "enum": [
                "before",
                "after"
              ],
              "description": "Before/after observation point for posedge or dual sampling; it does not change the raw waveform range."
            }
          },
          "required": [
            "clock",
            "rst_n",
            "awvalid",
            "awready",
            "awaddr",
            "awid",
            "awlen",
            "awsize",
            "awburst",
            "wvalid",
            "wready",
            "wdata",
            "wstrb",
            "wlast",
            "bvalid",
            "bready",
            "bid",
            "bresp",
            "arvalid",
            "arready",
            "araddr",
            "arid",
            "arlen",
            "arsize",
            "arburst",
            "rvalid",
            "rready",
            "rdata",
            "rid",
            "rresp",
            "rlast"
          ],
          "additionalProperties": false
        },
        "config_path": {
          "type": "string",
          "description": "Action-specific config_path input. Its type and accepted values are defined by this schema."
        },
        "name": {
          "type": "string",
          "description": "Saved-object name in this action namespace; do not assume names are shared by cursors, lists, and protocol configs."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ],
      "anyOf": [
        {
          "required": [
            "config"
          ]
        },
        {
          "required": [
            "config_path"
          ]
        }
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Load and validate an AXI interface configuration.",
  "x-purpose": "加载 AXI 配置。",
  "x-how_it_works": "保存 AXI channel 信号映射。",
  "x-when_to_use": "定义后续 AXI 查询对象。",
  "x-arg_contract_notes": "required: name; also one of: config / config_path",
  "x-description-zh": "加载 AXI 配置。",
  "x-agent": {
    "use_when": [
      "加载 AXI 配置。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 axi.analysis、axi.export 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "axi.analysis",
        "when": "汇总 AXI 行为。"
      },
      {
        "action": "axi.export",
        "when": "导出 AXI 数据。"
      }
    ],
    "constraints": [
      "required: name; also one of: config / config_path"
    ]
  }
}
