{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.counter.statistics.request.v1",
  "title": "counter.statistics request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "counter.statistics"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "required": [
        "clock",
        "time_range",
        "vld",
        "cnt"
      ],
      "properties": {
        "clock": {
          "type": "string",
          "description": "Action-specific clock input. Its type and accepted values are defined by this schema."
        },
        "cnt": {
          "type": "string",
          "description": "Action-specific cnt input. Its type and accepted values are defined by this schema."
        },
        "edge": {
          "type": "string",
          "description": "Clock sampling edge. The schema default is authoritative; negedge often matches monitor semantics."
        },
        "line_limit": {
          "type": "integer",
          "minimum": 1,
          "description": "Limits returned evidence rows only, not scanning, aggregation, or the verdict; read completeness fields as well."
        },
        "max_samples": {
          "type": "integer",
          "minimum": 1,
          "description": "Action-specific max_samples input. Its type and accepted values are defined by this schema."
        },
        "sample_point": {
          "type": "string",
          "enum": [
            "before",
            "after"
          ],
          "description": "Before/after observation point for posedge or dual sampling; it does not change the raw waveform range."
        },
        "time_range": {
          "type": "object",
          "properties": {
            "begin": {
              "type": "string",
              "description": "Action-specific begin input. Its type and accepted values are defined by this schema."
            },
            "end": {
              "type": "string",
              "description": "Action-specific end input. Its type and accepted values are defined by this schema."
            }
          },
          "additionalProperties": false,
          "description": "Closed analysis interval. begin and end may be omitted independently to use the available waveform bounds."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        },
        "vld": {
          "oneOf": [
            {
              "type": "string",
              "description": "Signal path or expression defining the valid sampling condition for counter statistics."
            },
            {
              "type": "object",
              "description": "Signal path or expression defining the valid sampling condition for counter statistics."
            }
          ],
          "description": "Signal path or expression defining the valid sampling condition for counter statistics."
        }
      },
      "additionalProperties": false
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Analyze sampled counter behavior and statistics.",
  "x-purpose": "统计计数器行为。",
  "x-how_it_works": "按 clock/vld/cnt 采样，分析递增、回绕、停顿和异常。",
  "x-when_to_use": "判断计数器是否符合预期。",
  "x-arg_contract_notes": "required: clock, time_range, vld, cnt",
  "x-description-zh": "统计计数器行为。",
  "x-agent": {
    "use_when": [
      "需要按采样 clock 统计一个 counter 的增量、回绕或活动。"
    ],
    "do_not_use_when": [
      "需要多个一般信号的活动统计或原始变化列表。"
    ],
    "alternatives": [
      {
        "action": "signal.statistics",
        "when": "需要一般信号而非 counter 语义的统计。"
      }
    ],
    "constraints": [
      "required: clock, time_range, vld, cnt"
    ]
  }
}
