{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.signal.changes.request.v1",
  "title": "signal.changes request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "signal.changes"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "aggregate_only": {
          "type": "boolean",
          "description": "Return only counts, first/last/final values, and warnings; omit data.changes rows."
        },
        "line_limit": {
          "type": "integer",
          "minimum": 1,
          "description": "Limits returned evidence rows only, not scanning, aggregation, or the verdict; read completeness fields as well."
        },
        "mode": {
          "type": "string",
          "description": "Return mode: timeline emits each change evidence, while summary emits aggregate facts only. Do not combine it with aggregate_only.",
          "enum": [
            "timeline",
            "summary"
          ],
          "default": "timeline"
        },
        "signal": {
          "type": "string",
          "description": "Final leaf signal path. Aggregate, array, and struct roots are not expanded automatically."
        },
        "time_range": {
          "type": "object",
          "properties": {
            "begin": {
              "type": "string",
              "description": "Action-specific begin input. Its type and accepted values are defined by this schema."
            },
            "end": {
              "type": "string",
              "description": "Action-specific end input. Its type and accepted values are defined by this schema."
            }
          },
          "additionalProperties": false,
          "description": "Closed analysis interval. begin and end may be omitted independently to use the available waveform bounds."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        },
        "value_format": {
          "type": "string",
          "enum": [
            "hex",
            "bin",
            "dec"
          ],
          "description": "Action-specific value_format input. Its type and accepted values are defined by this schema."
        }
      },
      "additionalProperties": false,
      "required": [
        "signal"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "List raw waveform value changes for a signal.",
  "x-purpose": "读取信号变化点。",
  "x-how_it_works": "扫描 FSDB value changes，支持窗口、line_limit 和聚合。",
  "x-when_to_use": "看信号何时跳变。",
  "x-arg_contract_notes": "required: signal",
  "x-description-zh": "读取信号变化点。",
  "x-agent": {
    "use_when": [
      "需要一个信号的原始波形变化时间线或聚合变化统计。"
    ],
    "do_not_use_when": [
      "需要按 clock edge 的协议语义采样。"
    ],
    "alternatives": [
      {
        "action": "event.find",
        "when": "需要按 clock 对表达式采样。"
      }
    ],
    "constraints": [
      "required: signal"
    ]
  }
}
