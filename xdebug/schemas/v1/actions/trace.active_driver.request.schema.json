{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.trace.active_driver.request.v1",
  "title": "trace.active_driver request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "trace.active_driver"
      ]
    },
    "target": {
      "type": "object",
      "properties": {
        "daidir": {
          "type": "string"
        },
        "fsdb": {
          "type": "string"
        },
        "session_id": {
          "type": "string"
        }
      }
    },
    "args": {
      "type": "object",
      "properties": {
        "limits": {
          "type": "object",
          "description": "Execution resource limits. Use this action's top-level limits properties, never args.limits."
        },
        "signal": {
          "type": "string",
          "description": "Final leaf signal path. Aggregate, array, and struct roots are not expanded automatically."
        },
        "time": {
          "type": "string",
          "description": "Target sample time. Prefer a canonical string with a unit; a bare number is interpreted only with time_unit."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        }
      },
      "additionalProperties": false,
      "required": [
        "signal",
        "time"
      ]
    },
    "limits": {
      "type": "object",
      "description": "Resource limits for the recursive active trace",
      "properties": {
        "max_depth": {
          "type": "integer",
          "minimum": 1,
          "description": "Maximum recursion depth through pass-through assignments and aliases (default 8)"
        },
        "max_nodes": {
          "type": "integer",
          "minimum": 1,
          "description": "Maximum number of trace nodes before truncation (default 50)"
        },
        "max_alias_candidates": {
          "type": "integer",
          "minimum": 0,
          "description": "Maximum number of alias candidates to explore (default 8)"
        },
        "max_trace_signals": {
          "type": "integer",
          "minimum": 1,
          "description": "Maximum signal count for value lookup (default 64)"
        }
      },
      "additionalProperties": false
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Find active driver evidence at a specific waveform time.",
  "x-purpose": "在指定时间找 active driver。",
  "x-how_it_works": "结合 waveform 当前值和 design 依赖，定位 time 的有效驱动证据。",
  "x-when_to_use": "回答“此刻是谁真正驱动了它”。",
  "x-arg_contract_notes": "required: signal, time",
  "x-description-zh": "在指定时间找 active driver。",
  "x-agent": {
    "use_when": [
      "在指定时间找 active driver。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 trace.active_driver_chain、trace.driver 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "trace.active_driver_chain",
        "when": "展开 active driver 链。"
      },
      {
        "action": "trace.driver",
        "when": "查找信号直接 driver。"
      }
    ],
    "constraints": [
      "required: signal, time"
    ]
  }
}
