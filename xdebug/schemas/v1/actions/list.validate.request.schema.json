{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.list.validate.request.v1",
  "title": "list.validate request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "list.validate"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Saved-object name in this action namespace; do not assume names are shared by cursors, lists, and protocol configs."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Validate signal paths stored in a waveform list.",
  "x-purpose": "验证 list 中信号是否存在。",
  "x-how_it_works": "逐条检查 FSDB signal handle。",
  "x-when_to_use": "发现过期或错误路径。",
  "x-arg_contract_notes": "required: name",
  "x-description-zh": "验证 list 中信号是否存在。",
  "x-agent": {
    "use_when": [
      "验证 list 中信号是否存在。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 list.add、list.create 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "list.add",
        "when": "向信号列表追加一个信号。"
      },
      {
        "action": "list.create",
        "when": "创建命名信号列表。"
      }
    ],
    "constraints": [
      "required: name"
    ]
  }
}
