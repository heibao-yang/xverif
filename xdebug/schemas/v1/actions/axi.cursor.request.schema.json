{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.axi.cursor.request.v1",
  "title": "axi.cursor request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "axi.cursor"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "direction": {
          "type": "string",
          "enum": [
            "write",
            "read",
            "all"
          ]
        },
        "name": {
          "type": "string",
          "description": "已保存配置、游标、列表或接口配置名称。"
        },
        "op": {
          "type": "string",
          "enum": [
            "begin",
            "next",
            "prev",
            "pre",
            "last"
          ],
          "description": "游标移动或协议浏览操作。"
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "op"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Move a cursor across AXI transactions.",
  "x-purpose": "在 AXI transfer 间移动游标。",
  "x-how_it_works": "基于 AXI 查询结果按 op/direction 定位事务。",
  "x-when_to_use": "交互式浏览 AXI 事务。",
  "x-arg_contract_notes": "required: name, op",
  "x-description-zh": "在 AXI transfer 间移动游标。"
}
