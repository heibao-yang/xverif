{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.session.open.request.v1",
  "title": "session.open request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "session.open"
      ]
    },
    "target": {
      "type": "object",
      "properties": {
        "run_manifest": {
          "type": "string",
          "description": "可选 xdebug.run-manifest.v1 路径；提供时在启动 engine 前严格校验 target 资源。"
        }
      }
    },
    "args": {
      "type": "object",
      "properties": {
        "bind": {
          "type": "string"
        },
        "bind_host": {
          "type": "string"
        },
        "host": {
          "type": "string"
        },
        "name": {
          "type": "string",
          "description": "已保存配置、游标、列表或接口配置名称。"
        },
        "port": {
          "type": "integer"
        },
        "session_id": {
          "type": "string",
          "description": "目标 xdebug session 标识。"
        },
        "transport": {
          "type": "string",
          "enum": [
            "uds",
            "tcp",
            "file"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Open a design, waveform, or combined debug session.",
  "x-purpose": "打开 design/waveform session。",
  "x-how_it_works": "解析 target 中的 daidir/fsdb；可选 target.run_manifest 会在启动前校验 published state、canonical path、size 与 SHA-256。",
  "x-when_to_use": "建立后续 design/waveform 查询的资源上下文。",
  "x-arg_contract_notes": "required: name",
  "x-description-zh": "打开 design/waveform session。"
}
