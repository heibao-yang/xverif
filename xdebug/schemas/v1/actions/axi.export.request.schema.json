{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.axi.export.request.v1",
  "title": "axi.export request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "axi.export"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "Saved-object name in this action namespace; do not assume names are shared by cursors, lists, and protocol configs."
        },
        "output": {
          "type": "object",
          "properties": {
            "path": {
              "type": "string",
              "description": "Action-specific path input. Its type and accepted values are defined by this schema."
            },
            "file_format": {
              "type": "string",
              "enum": [
                "tsv",
                "csv"
              ],
              "description": "axi.export file format."
            },
            "verbose": {
              "type": "boolean",
              "description": "Action-specific verbose input. Its type and accepted values are defined by this schema."
            }
          },
          "additionalProperties": false,
          "description": "Export destination and rendering controls. path, file_format, and verbose are supported only where this action declares them."
        },
        "time_range": {
          "type": "object",
          "properties": {
            "begin": {
              "type": "string",
              "description": "Action-specific begin input. Its type and accepted values are defined by this schema."
            },
            "end": {
              "type": "string",
              "description": "Action-specific end input. Its type and accepted values are defined by this schema."
            }
          },
          "additionalProperties": false,
          "description": "Closed analysis interval. begin and end may be omitted independently to use the available waveform bounds."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        }
      },
      "additionalProperties": false,
      "anyOf": [
        {
          "required": [
            "time_range"
          ]
        }
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Export AXI transactions for external analysis.",
  "x-purpose": "导出 AXI 数据。",
  "x-how_it_works": "按 name 和 time_range 查询 AXI，再按 format/output.path 写出。",
  "x-when_to_use": "给外部表格或脚本分析。",
  "x-arg_contract_notes": "required: name; also one of: time_range",
  "x-description-zh": "导出 AXI 数据。",
  "x-agent": {
    "use_when": [
      "导出 AXI 数据。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 axi.analysis、axi.channel_stall 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "axi.analysis",
        "when": "汇总 AXI 行为。"
      },
      {
        "action": "axi.channel_stall",
        "when": "实验性 AXI stall 分析。"
      }
    ],
    "constraints": [
      "required: name; also one of: time_range"
    ]
  }
}
