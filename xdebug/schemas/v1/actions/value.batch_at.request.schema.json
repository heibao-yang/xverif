{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.value.batch_at.request.v1",
  "title": "value.batch_at request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "value.batch_at"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "clock": {
          "type": "string",
          "description": "Action-specific clock input. Its type and accepted values are defined by this schema."
        },
        "edge": {
          "type": "string",
          "description": "Clock sampling edge. The schema default is authoritative; negedge often matches monitor semantics."
        },
        "format": {
          "type": "string",
          "enum": [
            "h",
            "hex",
            "b",
            "bin",
            "binary",
            "d",
            "dec",
            "decimal",
            "array_indexed",
            "json",
            "tsv",
            "csv",
            "u64bin"
          ],
          "description": "Action-specific format input. Its type and accepted values are defined by this schema."
        },
        "sample_point": {
          "type": "string",
          "enum": [
            "before",
            "after"
          ],
          "description": "Before/after observation point for posedge or dual sampling; it does not change the raw waveform range."
        },
        "signals": {
          "type": "array",
          "items": {
            "type": "string",
            "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
          },
          "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
        },
        "slice_hint": {
          "type": "object",
          "description": "Structured slice_hint configuration. Only the declared properties are accepted."
        },
        "time": {
          "type": "string",
          "description": "Target sample time. Prefer a canonical string with a unit; a bare number is interpreted only with time_unit."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        },
        "value_format": {
          "type": "string",
          "enum": [
            "hex",
            "bin",
            "dec"
          ],
          "description": "Action-specific value_format input. Its type and accepted values are defined by this schema."
        }
      },
      "additionalProperties": false,
      "required": [
        "signals",
        "time",
        "clock"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Read multiple signal values at one sampled waveform time.",
  "x-purpose": "批量读取多个信号值。",
  "x-how_it_works": "对 signals 列表按同一 clock/time 执行 FSDB 读取。",
  "x-when_to_use": "减少多信号同一时刻检查的开销。",
  "x-arg_contract_notes": "required: signals, time, clock",
  "x-description-zh": "批量读取多个信号值。",
  "x-agent": {
    "use_when": [
      "批量读取多个信号值。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 value.at 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "value.at",
        "when": "读取单个信号在指定时间的值。"
      }
    ],
    "constraints": [
      "required: signals, time, clock"
    ]
  }
}
