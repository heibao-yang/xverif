{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.session.kill.request.v1",
  "title": "session.kill request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "target"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "session.kill"
      ]
    },
    "target": {
      "type": "object",
      "required": [
        "session_id"
      ],
      "properties": {
        "session_id": {
          "type": "string",
          "description": "目标 xdebug session 标识。"
        }
      },
      "additionalProperties": false
    },
    "args": {
      "type": "object",
      "properties": {
        "session_id": {
          "type": "string",
          "description": "目标 xdebug session 标识。"
        }
      },
      "additionalProperties": false,
      "required": []
    },
    "limits": {
      "type": "object"
    },
    "output": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "session.kill: 强制移除指定 session。",
  "x-purpose": "强制移除指定 session。",
  "x-how_it_works": "按 target.session_id 清理记录和关联资源。",
  "x-when_to_use": "处理异常残留 session。",
  "x-arg_contract_notes": "required: target.session_id"
}
