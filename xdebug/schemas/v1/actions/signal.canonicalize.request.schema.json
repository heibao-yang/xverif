{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.signal.canonicalize.request.v1",
  "title": "signal.canonicalize request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "signal.canonicalize"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "signal": {
          "type": "string",
          "description": "Final leaf signal path. Aggregate, array, and struct roots are not expanded automatically."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        }
      },
      "additionalProperties": false,
      "required": [
        "signal"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Return the canonical design path for a signal.",
  "x-purpose": "返回信号 canonical 名称。",
  "x-how_it_works": "基于设计解析结果选出规范路径。",
  "x-when_to_use": "让后续 action 使用稳定路径。",
  "x-arg_contract_notes": "required: signal",
  "x-description-zh": "返回信号 canonical 名称。",
  "x-agent": {
    "use_when": [
      "返回信号 canonical 名称。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 signal.changes、signal.resolve 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "signal.changes",
        "when": "读取信号变化点。"
      },
      {
        "action": "signal.resolve",
        "when": "解析设计信号。"
      }
    ],
    "constraints": [
      "required: signal"
    ]
  }
}
