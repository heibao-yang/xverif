{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.list.diff.request.v1",
  "title": "list.diff request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "list.diff"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Saved-object name in this action namespace; do not assume names are shared by cursors, lists, and protocol configs."
        },
        "time_range": {
          "type": "object",
          "properties": {
            "begin": {
              "type": "string",
              "description": "Action-specific begin input. Its type and accepted values are defined by this schema."
            },
            "end": {
              "type": "string",
              "description": "Action-specific end input. Its type and accepted values are defined by this schema."
            }
          },
          "additionalProperties": false,
          "description": "Closed analysis interval. begin and end may be omitted independently to use the available waveform bounds."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        },
        "value_format": {
          "type": "string",
          "enum": [
            "hex",
            "bin",
            "dec"
          ],
          "description": "Action-specific value_format input. Its type and accepted values are defined by this schema."
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "time_range"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Find the first difference in a waveform list over a time range.",
  "x-purpose": "查找 list 在时间窗口内的首次差异。",
  "x-how_it_works": "解析 time_range.begin/end 后扫描 list 内信号变化。",
  "x-when_to_use": "定位一组信号何时开始不同。",
  "x-arg_contract_notes": "required: name, time_range",
  "x-description-zh": "查找 list 在时间窗口内的首次差异。",
  "x-agent": {
    "use_when": [
      "查找 list 在时间窗口内的首次差异。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 list.add、list.create 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "list.add",
        "when": "向信号列表追加一个信号。"
      },
      {
        "action": "list.create",
        "when": "创建命名信号列表。"
      }
    ],
    "constraints": [
      "required: name, time_range"
    ]
  }
}
