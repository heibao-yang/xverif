{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.session.close.request.v1",
  "title": "session.close request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "target"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "session.close"
      ]
    },
    "target": {
      "type": "object",
      "required": [
        "session_id"
      ],
      "properties": {
        "session_id": {
          "type": "string",
          "description": "目标 xdebug session 标识。"
        }
      },
      "additionalProperties": false
    },
    "args": {
      "type": "object",
      "properties": {
        "session_id": {
          "type": "string",
          "description": "目标 xdebug session 标识。"
        }
      },
      "additionalProperties": false,
      "required": []
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "session.close: 关闭指定 session。",
  "x-purpose": "关闭指定 session。",
  "x-how_it_works": "释放该 session 的 runtime 资源并从管理器移除。",
  "x-when_to_use": "结束不再使用的调试上下文。",
  "x-arg_contract_notes": "required: target.session_id"
}
