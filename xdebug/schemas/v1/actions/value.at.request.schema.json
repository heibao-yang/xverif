{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.value.at.request.v1",
  "title": "value.at request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "value.at"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "clock": {
          "type": "string",
          "description": "Action-specific clock input. Its type and accepted values are defined by this schema."
        },
        "edge": {
          "type": "string",
          "description": "Clock sampling edge. The schema default is authoritative; negedge often matches monitor semantics."
        },
        "format": {
          "type": "string",
          "enum": [
            "h",
            "hex",
            "b",
            "bin",
            "binary",
            "d",
            "dec",
            "decimal"
          ],
          "description": "Low-level display format for the sampled value. Only hexadecimal, binary, and decimal forms are accepted; export formats are invalid.",
          "default": "h"
        },
        "sample_point": {
          "type": "string",
          "enum": [
            "before",
            "after"
          ],
          "description": "Before/after observation point for posedge or dual sampling; it does not change the raw waveform range."
        },
        "signal": {
          "type": "string",
          "description": "Final leaf signal path. Aggregate, array, and struct roots are not expanded automatically."
        },
        "slice_hint": {
          "type": "object",
          "description": "Structured slice_hint configuration. Only the declared properties are accepted."
        },
        "time": {
          "type": "string",
          "description": "Target sample time. Prefer a canonical string with a unit; a bare number is interpreted only with time_unit."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        },
        "value_format": {
          "type": "string",
          "enum": [
            "hex",
            "bin",
            "dec"
          ],
          "description": "Action-specific value_format input. Its type and accepted values are defined by this schema."
        }
      },
      "additionalProperties": false,
      "required": [
        "signal",
        "time",
        "clock"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Read one signal value at a sampled waveform time.",
  "x-purpose": "读取单个信号在指定时间的值。",
  "x-how_it_works": "解析 time/clock，定位 FSDB signal handle，读取该时刻值并格式化。",
  "x-when_to_use": "回答某个时间点信号是什么。",
  "x-arg_contract_notes": "required: signal, time, clock",
  "x-description-zh": "读取单个信号在指定时间的值。",
  "x-agent": {
    "use_when": [
      "需要一个最终叶子信号在单一采样时刻的值。"
    ],
    "do_not_use_when": [
      "需要原始值变化时间线。",
      "需要多信号布尔表达式求值。"
    ],
    "alternatives": [
      {
        "action": "signal.changes",
        "when": "需要每次原始值变化。"
      },
      {
        "action": "expr.eval_at",
        "when": "需要在同一采样时刻求多信号表达式。"
      }
    ],
    "constraints": [
      "required: signal, time, clock"
    ]
  }
}
