{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.cursor.get.request.v1",
  "title": "cursor.get request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "cursor.get"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Saved-object name in this action namespace; do not assume names are shared by cursors, lists, and protocol configs."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Read a named waveform cursor.",
  "x-purpose": "读取命名游标。",
  "x-how_it_works": "从 CursorManager 按 name 取出保存的时间和元信息。",
  "x-when_to_use": "把自然语言中的“刚才那个点”变成确定时间。",
  "x-arg_contract_notes": "required: name",
  "x-description-zh": "读取命名游标。",
  "x-agent": {
    "use_when": [
      "读取命名游标。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 cursor.delete、cursor.list 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "cursor.delete",
        "when": "删除游标。"
      },
      {
        "action": "cursor.list",
        "when": "列出游标。"
      }
    ],
    "constraints": [
      "required: name"
    ]
  }
}
