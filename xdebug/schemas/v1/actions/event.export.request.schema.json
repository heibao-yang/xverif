{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xdebug.event.export.request.v1",
  "title": "event.export request",
  "type": "object",
  "required": [
    "api_version",
    "action",
    "args"
  ],
  "properties": {
    "api_version": {
      "type": "string",
      "enum": [
        "xdebug.v1"
      ]
    },
    "request_id": {
      "type": "string"
    },
    "action": {
      "type": "string",
      "enum": [
        "event.export"
      ]
    },
    "target": {
      "type": "object"
    },
    "args": {
      "type": "object",
      "properties": {
        "aggregate": {
          "type": "object",
          "description": "Structured aggregate configuration. Only the declared properties are accepted.",
          "properties": {
            "events": {
              "type": "boolean",
              "default": true,
              "description": "Action-specific events input. Its type and accepted values are defined by this schema."
            },
            "group_by": {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1,
                "description": "Action-specific group_by input. Its type and accepted values are defined by this schema."
              },
              "uniqueItems": true,
              "description": "Ordered group_by entries. Each item must satisfy the declared item contract."
            }
          },
          "additionalProperties": false
        },
        "clock": {
          "type": "string",
          "description": "Action-specific clock input. Its type and accepted values are defined by this schema."
        },
        "edge": {
          "type": "string",
          "description": "Clock sampling edge. The schema default is authoritative; negedge often matches monitor semantics."
        },
        "expr": {
          "type": "string",
          "description": "Action-specific expr input. Its type and accepted values are defined by this schema."
        },
        "line_limit": {
          "type": "integer",
          "minimum": 1,
          "description": "Limits returned evidence rows only, not scanning, aggregation, or the verdict; read completeness fields as well."
        },
        "max_events": {
          "type": "integer",
          "minimum": 1,
          "description": "Action-specific max_events input. Its type and accepted values are defined by this schema."
        },
        "max_samples": {
          "type": "integer",
          "minimum": 1,
          "description": "Action-specific max_samples input. Its type and accepted values are defined by this schema."
        },
        "mode": {
          "type": "string",
          "enum": [
            "export"
          ],
          "default": "export",
          "description": "Action processing or return mode. Its legal values, default, and interactions are action-specific."
        },
        "name": {
          "type": "string",
          "description": "Saved-object name in this action namespace; do not assume names are shared by cursors, lists, and protocol configs."
        },
        "output": {
          "type": "object",
          "properties": {
            "path": {
              "type": "string",
              "description": "Action-specific path input. Its type and accepted values are defined by this schema."
            },
            "file_format": {
              "type": "string",
              "enum": [
                "json"
              ],
              "description": "event.export file format."
            },
            "verbose": {
              "type": "boolean",
              "description": "Action-specific verbose input. Its type and accepted values are defined by this schema."
            }
          },
          "additionalProperties": false,
          "description": "Export destination and rendering controls. path, file_format, and verbose are supported only where this action declares them."
        },
        "rst_n": {
          "type": "string",
          "description": "Action-specific rst_n input. Its type and accepted values are defined by this schema."
        },
        "sample_point": {
          "type": "string",
          "enum": [
            "before",
            "after"
          ],
          "description": "Before/after observation point for posedge or dual sampling; it does not change the raw waveform range."
        },
        "signals": {
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string",
                "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
              },
              "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
            },
            {
              "type": "object",
              "additionalProperties": {
                "type": "string",
                "description": "Value for a caller-defined signal alias key. Supply the real signal path or the action-supported expression for that alias.",
                "x-dynamic-contract": "Each property key is an alias referenced by this action; each value resolves that alias to a signal path or supported expression."
              },
              "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
            }
          ],
          "description": "Signal-path list or alias-to-path/expression map. Expressions must reference aliases rather than nested paths."
        },
        "time_range": {
          "type": "object",
          "properties": {
            "begin": {
              "type": "string",
              "description": "Action-specific begin input. Its type and accepted values are defined by this schema."
            },
            "end": {
              "type": "string",
              "description": "Action-specific end input. Its type and accepted values are defined by this schema."
            }
          },
          "additionalProperties": false,
          "description": "Closed analysis interval. begin and end may be omitted independently to use the available waveform bounds."
        },
        "time_unit": {
          "type": "string",
          "enum": [
            "auto",
            "fs",
            "ps",
            "ns",
            "us",
            "ms",
            "s"
          ],
          "description": "Interprets only unitless time numbers; it never overrides a time string that already has a unit."
        }
      },
      "additionalProperties": false,
      "required": [
        "expr"
      ],
      "anyOf": [
        {
          "required": [
            "name"
          ]
        },
        {
          "required": [
            "clock",
            "signals"
          ]
        }
      ]
    },
    "limits": {
      "type": "object"
    },
    "auth_token": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "trace_id": {
      "type": "string"
    },
    "span_id": {
      "type": "string"
    },
    "parent_span_id": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Export sampled events that match an expression.",
  "x-purpose": "导出满足表达式的事件。",
  "x-how_it_works": "与 event.find 同样分析，但按 export 模式返回/写出更多命中数据；表达式支持布尔组合、相等比较和大小比较。",
  "x-when_to_use": "把事件列表交给后处理或报告。",
  "x-arg_contract_notes": "required: expr; also one of: name / clock + signals",
  "x-description-zh": "导出满足表达式的事件。",
  "x-agent": {
    "use_when": [
      "导出满足表达式的事件。"
    ],
    "do_not_use_when": [
      "不要把本 action 用于 event.config.list、event.config.load 的业务目标；请改用列出的同域 action。"
    ],
    "alternatives": [
      {
        "action": "event.config.list",
        "when": "列出事件配置。"
      },
      {
        "action": "event.config.load",
        "when": "保存事件查询配置。"
      }
    ],
    "constraints": [
      "required: expr; also one of: name / clock + signals"
    ]
  }
}
